global:
  scrape_interval: 15s
  evaluation_interval: 15s
  external_labels:
    platform: openrag

rule_files:
  - /etc/prometheus/alerts.yaml

scrape_configs:
  - job_name: openrag-otel-collector
    static_configs:
      - targets: [otel-collector:8889]
